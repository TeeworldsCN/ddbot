// import { TextHandler } from "./bottype";

// export const find: TextHandler = async (msg, bot, type, raw) => {
//     // 地图Query
//     const mapQuery = await msg.tools.api.get(
//       `/servers/players/?${encodeURIComponent(mapQueryString)}`
//     );
// };
